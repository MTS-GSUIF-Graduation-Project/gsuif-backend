package eg.mts.gsuif.entity;

/**
 * Persisted generation-run outcomes from T-14 {@code ck_gsuif_generation_run_status}.
 *
 * <p>Stored with {@code EnumType.STRING} — never ordinal.
 */
public enum GenerationRunStatus {
    SUCCESS,
    BUILD_FAILED
}
